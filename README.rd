## Getting Started

The easiest way to get started is to use the Devcontainer with VS Code, or GitHub Codespaces

Once in the container, open a terminal and run:

```
. /opt/ros/noetic/setup.sh
```

Then you can run typical rust commands like:

```
cargo run
```

Good Hunting.